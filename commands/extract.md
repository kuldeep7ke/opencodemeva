---
description: "Pull reusable tokens and components into the design system. Usage: /extract <target>. E.g.: /extract the card component"
---

Load the `impeccable` skill and run `/impeccable extract <target>`.
