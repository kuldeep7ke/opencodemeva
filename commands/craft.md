---
description: "End-to-end design and build: shape → design → implement → iterate. Usage: /craft <feature>. E.g.: /craft a pricing page for a developer tool"
---

Load the `impeccable` skill and run `/impeccable craft <feature>`.
